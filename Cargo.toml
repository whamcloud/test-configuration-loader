[package]
name = "test-configuration-loader"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.5"
serde_yaml = "0.9"
thiserror = "1.0"

notify = { version = "5.1", optional = true }

[features]
hot-reload = ["notify"]

[dev-dependencies]
tempfile = "3.3"
